# Farkle Project

This repository contains:
1. **backend**: A Telegram Bot for playing Farkle with friends.
2. **web-app**: (Potential frontend if needed, currently unused).

## Quick Start (Bot)
1. Go to `backend` directory.
2. Create `.env` from `.env.example`.
3. `npm install`.
4. `npm start`.

See `backend/INSTRUCTIONS.md` for more details.
